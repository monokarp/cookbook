import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    boxContainer: {
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "flex-end",
        padding: 10,
    },
    label: {
        marginHorizontal: 10,
        fontSize: 18,
    },
});
