import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    boxContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'flex-end',
        marginTop: 10
    },
    label: {
        marginHorizontal: 10,
        width: '15%'
    },
});
